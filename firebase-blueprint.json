{
  "entities": {
    "BirthdayContent": {
      "title": "BirthdayContent",
      "description": "Central persistent storage document containing all birthday celebration texts, media links, reveal photos, scrapbook memories, and love notes.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "recipient_name": { "type": "string" },
        "birthday_message": { "type": "string" },
        "background_url": { "type": "string" },
        "music_url": { "type": "string" },
        "reveal_photos": { "type": "array", "items": { "type": "string" } },
        "reveal_titles": { "type": "array" },
        "site_texts": { "type": "object" },
        "memories": { "type": "array" },
        "love_notes": { "type": "array" },
        "updated_at": { "type": "string" }
      },
      "required": ["recipient_name", "birthday_message"]
    }
  },
  "firestore": {
    "/birthday_content/{contentId}": {
      "entity": "BirthdayContent",
      "description": "Singleton configuration document ('main') holding the customizable celebration data for the website."
    }
  }
}
